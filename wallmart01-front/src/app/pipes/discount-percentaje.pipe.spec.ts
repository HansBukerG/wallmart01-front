import { DiscountPercentajePipe } from './discount-percentaje.pipe';

describe('DiscountPercentajePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountPercentajePipe();
    expect(pipe).toBeTruthy();
  });
});
